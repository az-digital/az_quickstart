langcode: en
status: true
dependencies:
  config:
    - crop.type.az_square_aspect_ratio
  module:
    - crop
name: az_thumbnail
label: 'AZ Thumbnail (Scales and crops to 100x100)'
effects:
    id: image_scale_and_crop
    weight: -9
    data:
      width: 100
      height: 100
      anchor: center-center
    id: crop_crop
    weight: -10
    data:
      crop_type: az_square_aspect_ratio
      automatic_crop_provider: null
