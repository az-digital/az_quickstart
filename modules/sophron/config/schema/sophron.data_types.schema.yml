# Basic data types for Sophron module.

sophron.mapping_command:
  type: mapping
  label: 'Mapping command'
  mapping:
    method:
      type: string
      label: 'Method callback'
    arguments:
      type: sequence
      label: 'The arguments to pass to the method'
      sequence:
        type: string
