langcode: en
status: true
dependencies:
  config:
    - key.key.az_eds_dn
    - ldap_servers.server.az_eds
id: az_eds_dn
label: 'EDS DN'
config_type: ldap_server
config_prefix: ldap_servers.server
config_name: az_eds
config_item: binddn
key_id: az_eds_dn
