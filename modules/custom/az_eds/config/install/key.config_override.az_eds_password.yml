langcode: en
status: true
dependencies:
  config:
    - key.key.eds_password
    - ldap_servers.server.az_eds
id: az_eds_password
label: 'EDS Password'
config_type: ldap_server
config_prefix: ldap_servers.server
config_name: az_eds
config_item: bindpw
key_id: eds_password
